import os
import tkinter as tk

# ==========================================
# FARKLI FLAG ADI (shgkfshgfg)
# ==========================================
FLAG = "C:\\Windows\\Temp\\shgkfshgfg.flag"

# Flag yoksa aç, varsa açma
if not os.path.exists(FLAG):
    # Flag oluştur (bir daha açılmasın)
    with open(FLAG, "w") as f:
        f.write("GÖSTERİLDİ")

    # ==========================================
    # PANEL OLUŞTUR
    # ==========================================
    window = tk.Tk()
    window.title("Talep Reddedildi!")
    window.geometry("500x350")
    window.resizable(False, False)
    window.configure(bg="#f0f0f0")

    tk.Label(
        window,
        text="🚫 Talep Reddedildi!",
        font=("Arial", 18, "bold"),
        fg="red",
        bg="#f0f0f0"
    ).pack(pady=15)

    aciklama = (
        "Sayın kullanıcı,\n\n"
        "Tarafınızdan gönderilen talep, kanıtlar dahilinde incelenmiştir.\n"
        "Yapılan incelemeler sonucunda, gönderilen mesajların IP adresi\n"
        "ile hesabınızda kayıtlı IP adresleri uyuşmamaktadır.\n\n"
        "Bu nedenle talebiniz reddedilmiştir.\n\n"
        "İyi günler dileriz."
    )
    tk.Label(
        window,
        text=aciklama,
        font=("Arial", 10),
        justify="left",
        wraplength=450,
        bg="#f0f0f0"
    ).pack(pady=10)

    def kapat():
        window.destroy()

    tk.Button(
        window,
        text="Tamam",
        command=kapat,
        bg="#4CAF50",
        fg="white",
        font=("Arial", 10, "bold"),
        width=15
    ).pack(pady=15)

    window.mainloop()
